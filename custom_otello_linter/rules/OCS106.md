# OCS106. Decorator @selectoleted or selectoleted(...) should not be presented

`@selectoleted` and `selectoleted(...)` are for local runs via selectolet (`make e2e-run`). They must not be committed.

Allowed locally; remove before merge to the main branch.
